<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4 mx-auto jumbotron center height-aside-profile">
    <section class="user_info">
      <%= gravatar_for @user %>
      <h1 class="center"><%= @user.name %></h1>
      <span><%= link_to "view my profile", @user, class: "btn btn-primary" %></span>
      <span><b>Posts:</b> <%= @user.posts.count %></span>
    </section>
    <hr>
    <section class="stats">
      <%= render 'shared/stats' %>
      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.each do |user| %>
            <%= link_to gravatar_for(user, size: 30), user %>
          <% end %>
        </div>
      <% end %>
    </section>
  </aside>
  <div class="col-md-8">
    <h3 class="center"><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>
